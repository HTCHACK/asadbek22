export const SET_TOUR_CATEGORY = (state, categories) => {
    state.categories = categories;
};

export const SET_USER = (state, user) => {
    state.user = user;
};

export const SET_CONTACTS = (state, contacts) => {
    state.contacts = contacts;
};

export const SET_ORDERS = (state, orders) => {
    state.orders = orders;
};

export const SET_GALLERY = (state, galleries) => {
    state.galleries = galleries;
};

export const SET_TOURS = (state, tours) => {
    state.tours = tours;
};

export const SET_COMMENTS = (state, comments) => {
    state.comments = comments;
};
